# FTP deploy credentials – copy to .env.ftp and fill in
# .env.ftp is gitignored (via .env*)
# Run: pnpm deploy:ftp

FTP_HOST=w0105bd5.kasserver.com
FTP_USER=your-ftp-username
FTP_PASSWORD=your-password
FTP_PATH=/fs26-front.felixseeger.de/

# Optional:
# FTP_PORT=21
# FTP_SECURE=false   # use plain FTP if FTPS fails
